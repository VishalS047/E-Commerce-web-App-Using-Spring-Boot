<!doctype html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">

<body>
	
	<div th:fragment="pdetails" th:remove="tag">
	
		<label for="">The following info is required to calculate the <Strong>shipping cost</Strong> for the product.</label>
		<p>The dimensions <strong>(L * W * H)</strong> is for the box that is used to pack the product.(Not the product's dimension)</p>
		
		<div class="form-group row ">
			<label for="len" class="col-sm-4 col-form-label">LENGTH (in <strong>inches</strong>)</label>
			<div class="col-sm-8">
				<input type="number" step="0.01" class="form-control" th:field="*{length}" name="len" id="len" required="required"  />
			</div>
		</div>
		
		<div class="form-group row ">
			<label for="width" class="col-sm-4 col-form-label">WIDTH (in <strong>inches</strong>)</label>
			<div class="col-sm-8">
				<input type="number" step="0.01" class="form-control" th:field="*{width}" name="width" id="width" required="required"  />
			</div>
		</div>
		<div class="form-group row ">
			<label for="height" class="col-sm-4 col-form-label">HEIGHT (in <strong>inches</strong>)</label>
			<div class="col-sm-8">
				<input type="number" step="0.01" class="form-control" th:field="*{height}" name="height" id="height" required="required"  />
			</div>
		</div>
		<div class="form-group row ">
			<label for="weight" class="col-sm-4 col-form-label">WEIGHT (in <strong>pounds</strong>)</label>
			<div class="col-sm-8">
				<input type="number" class="form-control" th:field="*{weight}" name="weight" id="weight" required="required"  />
			</div>
		</div>
		
	</div>
	
</body>
</html>